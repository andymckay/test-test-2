name: CI

on: [push]
env: 
  MSG: Hello

jobs:
  
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Run a one-line script
      run: echo $MSG
    - name: Maybe
      if: env.MSG
      run: echo Maybe $NOMSG
    - name: Maybe
      if: env.NOMSG
      run: echo Maybe $NOMSG




























